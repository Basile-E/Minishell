bash-5.2$ << test
> ^C
bash-5.2$ 

je ne gere pas le fait de ne pas avoir d'input avant << 

ca devrait retourner a la ligne et attendre un input mais je ne vois pas a quoi ca sert 
mais easy a implement 




typedef struct s_cmd {
    char **args;        // ["echo", "hello", NULL] 
    char *input_file;   // "input.txt" ou NULL
    char *output_file;  // "output.txt" ou NULL
    int append_mode;    // 1 si >>, 0 si >
    struct s_cmd *next; // Commande après |
} t_cmd;


token_to_cmd() : 
parcours la liste,
	premier elem doit etre un mot, il deviens la cmd, 
	puis jusquq qu'un operateur soir found il ajoute des lignes au ret_tab,
 		quand op is found
			check de pipe ou redirection si pipe on stop ici et passe a la prochaine cmd 
			else
 				on trouve le "sens" de la redirection et on set les params de la struct puis on passe a la prochaine cmd