bug chelou quand on reviens dans l'historique dans une str qui n'est pas terminer par un sep ou plusieurs sep 



next :
    parsing des token
        check error de base ()
        check de la syntaxe (pas de flag au debut, pas deux chevrons diff  )



Fonctions Utils :
    une fonction cleanup and exit qui prend en param la return value et un msg a return si besoin (permet de facilement fix des bug de bad return value)
    free tab qui prend un pointeur vers un tableau de char * et qui le free 
